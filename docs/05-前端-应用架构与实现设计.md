# 前端：应用架构与实现设计

## 前端目标

前端分为两个应用：

- `apps/web`：C 端内容前台，面向内容消费用户，负责首页信息流、热点榜、爆文榜、推荐榜、内容详情和阅读互动。
- `apps/admin`：B 端创作者后台，面向内容创作者，负责登录注册、创作工作台、Prompt 管理、素材管理、内容管理、审核结果和分发中心。

B 端承载 PDF 中创作者生产主链路，C 端承载发布后的内容消费与榜单分发。平台级运营后台只做后续预留，不进入当前 MVP。

## 目录建议

```text
apps/
├── web/                           # C 端内容前台
│   └── src/                       # C 端源码目录
│       ├── app/                   # Next.js App Router 页面
│       │   ├── page.tsx           # 首页信息流
│       │   ├── feed/              # 信息流页面
│       │   ├── rankings/          # 榜单页面目录
│       │   │   ├── hot/           # 热点榜页面
│       │   │   ├── viral/         # 爆文榜页面
│       │   │   └── recommend/     # 推荐榜页面
│       │   └── content/[id]/      # 内容详情页
│       ├── components/            # C 端组件目录
│       │   ├── feed/              # 信息流组件
│       │   ├── ranking/           # 榜单组件
│       │   ├── content/           # 内容详情组件
│       │   ├── layout/            # 页面布局组件
│       │   └── ui/                # 基础 UI 组件
│       ├── hooks/                 # C 端业务 hooks
│       ├── lib/                   # C 端基础工具
│       │   ├── api-client.ts      # API 请求封装
│       │   └── env.ts             # 环境变量读取
│       ├── stores/                # C 端状态管理
│       └── styles/                # 全局样式
└── admin/                         # B 端创作者后台
    └── src/                       # B 端源码目录
        ├── pages/                 # B 端页面路由
        │   ├── login/             # 登录页
        │   ├── register/          # 注册页
        │   ├── dashboard/         # 创作工作台
        │   ├── create/            # 新建创作页
        │   ├── create/:draftId/   # 草稿编辑页
        │   ├── drafts/            # 草稿箱
        │   ├── prompts/           # Prompt 管理
        │   ├── materials/         # 素材管理
        │   ├── contents/          # 内容管理
        │   ├── review/:contentId/ # 审核结果页
        │   └── distribution/      # 分发中心
        ├── components/            # B 端组件目录
        │   ├── editor/            # 富文本编辑器组件
        │   ├── ai/                # AI 创作组件
        │   ├── moderation/        # 审核结果组件
        │   ├── quality/           # 质量评分组件
        │   ├── table/             # 表格组件
        │   ├── form/              # 表单组件
        │   ├── layout/            # 页面布局组件
        │   └── ui/                # 基础 UI 组件
        ├── lib/                   # B 端基础工具
        │   ├── api-client.ts      # API 请求封装
        │   ├── indexed-db.ts      # IndexedDB 封装
        │   ├── sync-drafts.ts     # 草稿同步逻辑
        │   └── env.ts             # 环境变量读取
        └── stores/                # B 端状态管理
```

## C 端内容前台页面职责

- `/` 或 `/feed`：首页信息流，展示推荐内容、热点入口和最新发布内容。
- `/rankings/hot`：热点榜，强调阅读热度、发布时间衰减和质量分。
- `/rankings/viral`：爆文榜，强调阅读、点赞、分享等互动增长。
- `/rankings/recommend`：推荐榜，强调质量分、基础热度和用户反馈预留因子。
- `/content/[id]`：内容详情。

## B 端创作者后台页面职责

- `/login`、`/register`：创作者登录注册。
- `/dashboard`：创作工作台，展示草稿、审核状态、发布内容和质量概览。
- `/create`：新建 AI 图文创作。
- `/create/:draftId`：编辑已有草稿，恢复本地或云端内容。
- `/drafts`：草稿箱，展示本地、云端、待同步、冲突状态。
- `/prompts`：Prompt 管理，支持系统 Prompt 和个人 Prompt。
- `/materials`：素材上传、素材库、标签和基础合规状态。
- `/contents`：内容管理，展示草稿、审核中、需改写、已发布、已更新内容。
- `/review/:contentId`：审核结果、质量评分、风险片段和合规改写。
- `/distribution`：分发中心，查看内容在热点榜、爆文榜、推荐榜中的表现和排序解释。

## 状态管理

B 端创作者后台建议使用 Zustand 管理本地交互状态：

- `authStore`：token、用户信息、登录状态。
- `editorStore`：当前草稿、标题、正文、素材、AI 候选内容。
- `syncStore`：网络状态、同步状态、冲突信息。
- `uiStore`：编辑器面板、弹窗、审核抽屉、分发中心筛选。

服务端数据使用 TanStack Query：

- 登录态和创作者信息。
- Prompt 列表。
- 素材列表。
- 草稿列表。
- 审核结果。
- 内容管理列表。
- 分发中心数据。

C 端内容前台以服务端数据和轻量交互为主：

- `rankingStore`：当前榜单类型、滚动位置和筛选条件。
- `interactionStore`：点赞、分享、浏览去重等轻量状态。
- TanStack Query 管理首页信息流、榜单分页和内容详情缓存。

## API Client

C 端和 B 端都统一封装 `fetch`：

- 有登录态时自动注入 token。
- 自动附带 requestId。
- 统一解析成功和错误响应。
- 统一处理 401、网络错误、AI 超时和业务错误码。
- 支持 cursor 分页。
- B 端额外支持表格分页、筛选参数、创作者登录态和内容归属错误处理。

## 编辑器设计

- 富文本编辑器建议使用 Tiptap。
- AI 生成结果先进入候选区，不直接覆盖正文。
- 用户确认后支持插入、替换、追加。
- 编辑过程中持续触发本地保存和云端自动保存。
- 发布前必须经过审核结果校验。

## 离线能力

离线能力主要服务于 B 端创作者后台，详见 [07-前端-离线草稿与同步设计.md](./07-前端-离线草稿与同步设计.md)。前端必须保证：

- 断网时可以继续编辑。
- IndexedDB 中保留最新本地内容。
- 恢复网络后自动同步。
- 冲突时展示本地、云端、复制新草稿三种处理方式。

## C 端性能

- 首页信息流和榜单首屏使用 SSR 或服务端预取。
- 首屏不加载编辑器和 AI 面板。
- 内容卡片图片固定比例。
- 使用 cursor 分页和无限滚动。
- LCP 目标不高于 2.5 秒。

## 前端验收

- C 端首页信息流、热点榜、爆文榜、推荐榜和内容详情页完整可用。
- B 端登录注册完整可用。
- B 端 AI 创作页可完成生成、编辑、保存、审核、发布。
- 断网编辑不丢稿。
- 审核结果和质量评分展示清晰。
- 榜单无限滚动流畅。
- 移动端主要页面无文字溢出和布局重叠。
